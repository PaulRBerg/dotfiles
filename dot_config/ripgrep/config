# Search dotfiles and dot-directories like .agents by default.
--hidden

# Search files even when ignore files would exclude them.
--no-ignore

# --hidden includes VCS internals unless they are explicitly excluded.
--glob=!.git
