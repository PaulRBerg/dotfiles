# Orphaned: npm reads $XDG_CONFIG_HOME/npm/npmrc (NPM_CONFIG_USERCONFIG in
# env_core.sh.tmpl), so the legacy ~/.npmrc is unused. See dot_config/npm/.
.npmrc

# Orphaned: Vite+ shell startup/PATH integration was removed.
.vite-plus

# Orphaned: tmux config is no longer managed by this source state.
.config/tmux/tmux.conf

# Orphaned: service-account 1Password setup was removed in favor of account mode.
.config/chezmoi/chezmoi-service.toml
.config/prb/bin/chezmoi-service

# Replaced by ~/.local/libexec/mcp/chrome-devtools.
.local/bin/prb_chrome_devtools_mcp
